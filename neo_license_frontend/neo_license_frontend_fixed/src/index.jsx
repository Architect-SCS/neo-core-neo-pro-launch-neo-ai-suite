import React from "react";
import ReactDOM from "react-dom/client";
import LicenseUnlock from "./neo_license_frontend.jsx";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <LicenseUnlock />
  </React.StrictMode>
);
